import { getPublicArticles } from "@/lib/public-api";
const escape=(value:string)=>value.replace(/[<>&'\"]/g,(char)=>({"<":"&lt;",">":"&gt;","&":"&amp;","'":"&apos;",'"':"&quot;"})[char]!);
export async function GET(){const base=process.env.NEXT_PUBLIC_APP_URL??"http://localhost:3000";const articles=await getPublicArticles({limit:"30"});const items=articles.items.map((article)=>`<item><title>${escape(article.title)}</title><link>${base}/artikel/${article.slug}</link><guid>${base}/artikel/${article.slug}</guid><description>${escape(article.excerpt)}</description>${article.publishedAt?`<pubDate>${new Date(article.publishedAt).toUTCString()}</pubDate>`:""}</item>`).join("");const xml=`<?xml version="1.0" encoding="UTF-8"?><rss version="2.0"><channel><title>Crypto Exist</title><link>${base}</link><description>Berita, analisis, dan edukasi crypto Indonesia.</description><language>id-ID</language>${items}</channel></rss>`;return new Response(xml,{headers:{"content-type":"application/rss+xml; charset=utf-8","cache-control":"public, max-age=300"}});}
